{
  "name": "wp-react",
  "author": "CodersAKL",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "docker-compose run --rm wp-headless install_wordpress",
    "start": "docker-compose up"
  },
  "devDependencies": {
    "@types/wpapi": "^1.1.0"
  }
}
